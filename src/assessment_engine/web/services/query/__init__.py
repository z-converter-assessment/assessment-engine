"""Query service 도메인 mixin 패키지.

6 도메인 mixin (server/metric/attention/environment/report/task) 을 정의하고,
조합 클래스 `QueryService` 는 상위 `web/services/query_service.py` 가 multiple inheritance 로 결합한다
(repo 계층 `db/repositories/query/query_repository.py` 와 동일 패턴).
"""
